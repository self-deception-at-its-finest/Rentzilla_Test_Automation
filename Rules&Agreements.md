## Team Notes

- ensure availability of test entities (create a fixture for them);
- use kebab-case names (use `-` instead of spaces);
- always use `;` in code;
- split tests into folders (`ui`, `header`, `footer`, `page-name`, etc);
- use `test.step`;
- use `describe` blocks only when it makes sense, otherwise one test = one file;
